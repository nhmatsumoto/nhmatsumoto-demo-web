import { Container } from '../../styles/GlobalStyled';
import { HomeTitle } from './style';

const Home = () => {
    return (
        <Container>
            <HomeTitle>Home page</HomeTitle>
        </Container>
    );
}

export default Home;